<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.t28.forest.sales.dao.SalesDepotDao">

    <!--查询全部仓库信息-->
    <select id="findDepots" resultType="com.t28.forest.shared.entity.DepotDTO">
        SELECT id,`name` FROM jsh_depot WHERE delete_Flag=0;
    </select>

</mapper>